# Vocab-CLI
CLI-based vocabulary trainer written in Python. Loads CSV files, quizzes vocabulary or irregular verbs in 20-card batches, supports arrow-key file selection, spaced repetition, typo tolerance, and intuitive in-session commands like :q, :skip, and :show.
